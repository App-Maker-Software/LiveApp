//
//  UpgradeLogic.swift
//  
//
//  Created by Joseph Hinkle on 4/5/21.
//
/*
/// Dictates how live views will upgrade themselves from one version to another.
public enum UpgradeLogic {
    /// Recommended for development use. This mode will update the views immediately, even while they are on the screen.
    case immediately(animate: Bool)
    
    /// Recommended for production use. This mode will update views next time their "onAppear" is fired. Views will persist using the old view models until they go off the screen and receive a new "onAppear" event, and they will even persist using the old view model even if other views of the same struct type are using the upgraded view.
    case onNextOnAppear
}
*/
